name: Pull featured JPGs from Drive (retired)
on:
  workflow_dispatch:
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired — use GOMA Pipeline (.github/workflows/goma-pipeline.yml)"
